[package]
name = "serde_with"
version = "1.14.0"
edition = "2021"
publish = false

[features]
default = ["macros"]
macros = ["dep:serde_with_macros"]
hex = []

[dependencies]
serde = "1.0"
serde_with_macros = { version = "=1.5.2", optional = true }
