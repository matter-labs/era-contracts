// SPDX-License-Identifier: MIT

pragma solidity 0.8.28;

// !!! GENERATED FILE — DO NOT EDIT !!!
// Generated by scripts/gen-registry.ts from gen-registry-manifest.example.json.
// Every value below is a compile-time constant: deploying this contract IS the commitment.
// Reproduce by re-running the generator on the audited manifest and diffing this source.

import {ICoreRegistry} from "contracts/upgrades/registry/ICoreRegistry.sol";
import {EcosystemContract} from "contracts/upgrades/registry/ContractIdentifiers.sol";

contract CoreRegistryV99 is ICoreRegistry {
    uint256 internal constant OLD_PROTOCOL_VERSION = 420906795008; // 0.98.0
    uint256 internal constant NEW_PROTOCOL_VERSION = 425201762304; // 0.99.0

    error RegistryUnknownKey();

    function oldProtocolVersion() external pure returns (uint256) {
        return OLD_PROTOCOL_VERSION;
    }

    function newProtocolVersion() external pure returns (uint256) {
        return NEW_PROTOCOL_VERSION;
    }

    function proxyAddress(EcosystemContract _contract) external pure returns (address) {
        if (_contract == EcosystemContract.Bridgehub)
            return address(uint160(0x00000000000000000000000000000000000000b001));
        if (_contract == EcosystemContract.L1AssetRouter)
            return address(uint160(0x00000000000000000000000000000000000000b002));
        if (_contract == EcosystemContract.MessageRoot)
            return address(uint160(0x00000000000000000000000000000000000000b003));
        revert RegistryUnknownKey();
    }

    function implAddress(EcosystemContract _contract, uint256 _protocolVersion) external pure returns (address) {
        if (_contract == EcosystemContract.Bridgehub && _protocolVersion == OLD_PROTOCOL_VERSION)
            return address(uint160(0x00000000000000000000000000000000000000b101));
        if (_contract == EcosystemContract.Bridgehub && _protocolVersion == NEW_PROTOCOL_VERSION)
            return address(uint160(0x00000000000000000000000000000000000000b201));
        if (_contract == EcosystemContract.L1AssetRouter && _protocolVersion == OLD_PROTOCOL_VERSION)
            return address(uint160(0x00000000000000000000000000000000000000b102));
        if (_contract == EcosystemContract.L1AssetRouter && _protocolVersion == NEW_PROTOCOL_VERSION)
            return address(uint160(0x00000000000000000000000000000000000000b202));
        if (_contract == EcosystemContract.MessageRoot && _protocolVersion == OLD_PROTOCOL_VERSION)
            return address(uint160(0x00000000000000000000000000000000000000b103));
        if (_contract == EcosystemContract.MessageRoot && _protocolVersion == NEW_PROTOCOL_VERSION)
            return address(uint160(0x00000000000000000000000000000000000000b103));
        if (_protocolVersion == OLD_PROTOCOL_VERSION || _protocolVersion == NEW_PROTOCOL_VERSION) return address(0);
        revert RegistryUnknownKey();
    }

    function ecosystemContractList() external pure returns (EcosystemContract[] memory list) {
        list = new EcosystemContract[](3);
        list[0] = EcosystemContract.Bridgehub;
        list[1] = EcosystemContract.L1AssetRouter;
        list[2] = EcosystemContract.MessageRoot;
    }

    function proxyAdmin() external pure returns (address) {
        return address(uint160(0x00000000000000000000000000000000000000a001));
    }

    function ctmRegistry(bool _isZKsyncOS) external pure returns (address) {
        return
            _isZKsyncOS
                ? address(uint160(0x00000000000000000000000000000000000000c002))
                : address(uint160(0x00000000000000000000000000000000000000c001));
    }

    function verifyAll() external view returns (bool) {
        if (
            address(uint160(0x00000000000000000000000000000000000000b201)).codehash !=
            0xf8b07b083341d3a7667e38718918d301f47d62f82d8186f4ccd7ed7424a64ef3
        ) return false;
        return true;
    }
}
