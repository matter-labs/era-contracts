# v0.34.0 chain states — TO BE GENERATED

Current-source state snapshots for this branch (genesis protocol version 0.34.0, see
`configs/genesis/*/latest.json`). Generated by `setup-and-dump-state.ts` — locally via
`yarn setup-and-dump`, or through the 'Regenerate Anvil Interop Chain States' workflow — as part
of the merge-time regeneration batch. Consumed by the interop tests and validated by the
`state-generation-check` CI job. See `../README.md` for the frozen-vs-current rules.
