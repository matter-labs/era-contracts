# MultisigCommitter deployment

This upgrade consists of only the upgrade of ValidatorTimelock to MultisigCommitter contract. Only ZKsync OS ValidatorTimelock is upgraded.

`deploy-scripts/upgrade/ValidatorTimelockUpgrade.sol` is used to execute this upgrade.

This upgrade allows 2FA enforcement on L1.
