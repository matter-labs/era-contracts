# Upgrade input for the frozen local chain committed in zksync-os-integration-tests
# (`tests/local-chains/`, currently v31.0 / chain 506), used by its v31 -> v33 upgrade test.
#
# Intentionally empty of values, for the reasons given in `local.toml` in this directory —
# nothing on the v33 path reads a key from an upgrade input. It exists because
# `--env zksync-os-integration-test` requires an input file to be present (see `local.toml` for
# why absence is refused rather than defaulted).
