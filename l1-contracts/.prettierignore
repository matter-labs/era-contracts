# zkstack-out holds generated ABI artifacts (copy-to-zkstack-out writes them without a
# trailing newline); the repo-root .prettierignore already excludes them, but `yarn fmt`
# runs from this directory, so it needs the exclusion here too. Mirrors root .prettierignore.
zkstack-out
