# Settlement-layer and Gateway overview

The settlement-layer abstraction and historical Gateway design are documented in
[the section index](./README.md). Production chain migrations are disabled in this release; the page
also distinguishes retained machinery from active protocol behavior.
